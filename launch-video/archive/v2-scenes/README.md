# Archived: v1/v2 scenes

Everything the v3 direction ("Everything → One", DIRECTION_V3.md) bans or replaces: the founder
illustrations, the 4 × 2 app grid, the tab strip, the tiles-into-mark reveal, the feature-per-screen
act, the full-frame gradient background. Kept for reference only. **Never import from here.**
It is excluded from the TypeScript build (tsconfig.json).
