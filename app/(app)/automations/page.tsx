// Automations — "What Zenboard does automatically" (MASTER_PRODUCT_PLAN §7P).
// Not in the sidebar nav; reached from Settings and ⌘K. Auth via (app) layout.
import { AutomationsView } from '@/components/settings/automations-view';

export default function AutomationsPage() {
  return <AutomationsView />;
}
